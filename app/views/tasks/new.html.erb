<h1>New Task</h1>

<%= form_for @task do |f| %>

	<div class="error">
		<% @task.errors.full_messages.each do |msg| %>
  		<li><%= msg %></li>
  	<% end %>
	</div>
		
	<div>
		<%= f.label :description %>
		<%= f.text_field :description %>
		<%= f.submit "Save"%>
	</div>
<% end %>